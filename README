Make sure python 3.8 is installed.

### One time setup

`pip install -r requirements.txt`
`python -m venv .venv`

### Add .env

1. Create openai token
2. `echo 'export OPENAI_API_KEY="<token>"' >> .env`

### To run locally

`source .venv/bin/activate`
`streamlit run qa.py`
